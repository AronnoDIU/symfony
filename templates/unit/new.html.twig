{% extends 'base.html.twig' %}

{% block title %}New Unit{% endblock %}

{% block body %}
    <h1>Create new Unit</h1>

    {{ include('unit/_form.html.twig') }}

    <a href="{{ path('app_unit_index') }}">back to list</a>
    {# Add link to redirect to the home page #}
    <a href="{{ path('admin') }}">Go to Admin Home</a>
{% endblock %}
